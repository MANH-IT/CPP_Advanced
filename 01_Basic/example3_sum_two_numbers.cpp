#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
	int first_number, second_number;
	cin >> first_number >> second_number;
	
	int sum = first_number + second_number;
	cout << "Sum of " << first_number << " and " << second_number << " is: " << sum << '\n';

    return 0;
}


