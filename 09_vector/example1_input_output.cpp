#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
	int n; 
	cin >> n;
	vector<int> numbers;
	for (int i = 0; i < n; i++){
		int x; cin >> x;
		numbers.push_back(x);
	}
	cout < "Elements in vector: ";
	for (size_t i = 0; i < number.size(); i++){
		cout << number[i] << " ";
	}
	cout <<"\n";
    return 0;
}


